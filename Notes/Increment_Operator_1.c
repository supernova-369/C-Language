#include <stdio.h>

int main() {
   int tuna = 20;
   printf("%d \n", tuna);
   tuna++;
   printf("%d \n",tuna);
   /* double ++ add 1 to its value similarly double--subtracts 1 from it
   */
    return 0;
}